<div class="container">
  <% if @winners.nil? %>
    <div class="container">
      <h2 class="text-center">La ronda no tiene ganadores</h2>
      <h2 class="text-center"><%= "El resultado de la ruleta fue #{@result}" %></h2>
    </div>
  <% else %>
    <div class="container">
      <h3 class="text-center"><%= "Esta ronda contó con #{@winners.count} ganadores" %></h3>
      <h3 class="text-center"><%= "El resultado de la ruleta fue #{@result}" %></h3>
    </div>
  <% end %>
  <%= link_to "nueva ronda", root_path, class: "btn btn-default btn-block" %>
</div>
